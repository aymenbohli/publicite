<form style="display: inline-block;" method="post" action="{{ path('annonce_delete', {'id': annonce.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ annonce.id) }}">
    <button class="btn btn-danger btn-sm"><i class="fa fa-close"></i></button>
</form>
<a href="{{ path('annonce_edit', {'id': annonce.id}) }}" class="btn btn-success btn-sm"><i class="fa fa-edit"></i></a>
